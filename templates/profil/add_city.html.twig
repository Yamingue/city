{% extends "base.html.twig" %}

{% block body %}
    <div class="ui container">
        {{ form_start(form)}}
        {{ form_widget(form)}}
        <button type="submit" class="ui labeled icon button primary">
            <i class="send icon"></i>
            Envoyer
        </button>
        {{ form_end(form)}}
    </div>
{% endblock %}