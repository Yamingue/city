{% extends 'base.html.twig' %}

{% block title %}TypeChabre{% endblock %}

{% block body %}
    <h1>TypeChabre</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ type_chabre.id }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ type_chabre.type }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('type_chabre_index') }}">back to list</a>

    <a href="{{ path('type_chabre_edit', {'id': type_chabre.id}) }}">edit</a>

    {{ include('type_chabre/_delete_form.html.twig') }}
{% endblock %}
